# Scheduler entry points

No lifecycle scheduler script was admitted during the structural migration.
Add only portable, reviewed wrappers that write mutable outputs to
`results/audit/` and contain no machine-specific absolute paths.
